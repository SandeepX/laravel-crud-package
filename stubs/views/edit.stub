
    <h1>Edit</h1>

    <form method="POST" action="{{ route('{{ routeName }}.update', ${{ variable }}) }}">
        @csrf
        @method('PUT')

        @include('{{ viewPath }}._form')
    </form>
